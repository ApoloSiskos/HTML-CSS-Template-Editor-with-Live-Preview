# HTML-CSS-Template-Editor-with-Live-Preview

With this tool, the user can customise an HTML/CSS template.
For a specific widget template, the user can fill in the input boxes given and live preview the result. 
In case, the user is familiar with HTML/CSS can change the code.
The result (HTML and CSS codes) can be copied to the clipboard with the buttons given in the page. 
